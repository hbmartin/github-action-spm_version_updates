# frozen_string_literal: true

source "https://rubygems.org"

gemspec

eval_gemfile "../../gemfiles/release_tooling.gemfile"

# Isolation bundle: proves the core gem and its specs load without
# danger, octokit, or any action-only dependencies.
group(:development, :test) {
  gem "rspec", "~> 3.0"
  # Exercises the optional Xcode-project mode in the core specs.
  gem "xcodeproj", "~> 1.24"
}
